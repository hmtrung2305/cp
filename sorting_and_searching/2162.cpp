#include <bits/stdc++.h>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(0);

    int n;
    std::cin >> n;

    std::set<int> s;
    for (int i = 1; i <= n; i++) {
        s.insert(i);
    }

    while (!s.empty()) {
        int u = ;
    }

    return 0;
}